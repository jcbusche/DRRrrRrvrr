describe('', function(){
  var ;
  beforeEach(module('zombieDrive'));
  beforeEach(inject(function($injector){
    = $injector.get('');
  }));
  it('should', function(){
    expect().toBe('');
  });
});
